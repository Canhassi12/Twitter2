import {FormLogin, TwitterLogo} from "../../components";

export default function() {
    return(
        <div>
            <TwitterLogo />            
            <FormLogin />
        </div>
    );
}